// Admin-specific configuration. Created on first deploy.
// The service role key lives in admin.html (since admin.html is the only
// page that needs it) and the password lives here as a separate file for
// hygiene. This file is what you'd edit to change the admin password.

window.ADMIN_CONFIG = {
  // CHANGE THIS to a real password before going live.
  password: 'XNoEpdzEqd1hEZwH'
};
